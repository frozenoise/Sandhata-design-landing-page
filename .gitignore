# dependencies
node_modules

# next.js build output
.next
out
next-env.d.ts

# secrets / env (keep .env.local.example tracked as a template)
.env
.env*.local

# local AI-tool config (may contain machine-specific settings)
.claude/
.codex/
.windsurf/
skills-lock.json

# markdown (notes, guides, prompt docs) — but keep the project README + handoff
*.md
!/readme.md
!/HANDOFF.md
!/packages/spectra/README.md
!/packages/spectra-tokens/README.md
!/packages/spectra/guidelines/**/*.md

# os / editor cruft
.DS_Store
*.log
npm-debug.log*

# misc local
_ds_bundle.js.map

/app/generated/prisma

# workspace package build output (generated, not source)
packages/*/dist
